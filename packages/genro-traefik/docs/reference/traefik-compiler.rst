Compiler
========

The compiler walks the Bag tree and produces a YAML-compatible dictionary.
It is intentionally minimal — all rendering logic lives in the builder's
``compile_*`` methods.

.. automodule:: genro_traefik.traefik_compiler
   :members:
   :undoc-members:

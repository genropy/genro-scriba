Compiler
========

The compiler converts the Bag tree to Ansible YAML format,
handling ``$var`` to ``{{ var }}`` conversion and ``args_*`` flattening.

.. automodule:: genro_ansible.ansible_compiler
   :members:
   :undoc-members:
